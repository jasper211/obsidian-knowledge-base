---
type: methodology
project: OB知识库
status: 生效
as_of: 2026-08-28
owner: Codex
---

# OB自治循环机制

这份机制用于让 `OB` 的推进从“等你确认下一步”切换为“按固定回路持续推进”，直到当前项目治理完成，再进入持续优化。

## 一、总原则

后续默认不再按“你确认一次，我做一步”的方式推进。

默认执行方式改为：

`选下一簇 -> 读取最小变更面 -> 治理 -> 验证 -> 记录 -> 自动进入下一簇`

只有遇到下面 4 类情况才暂停：

- 出现互相冲突的现行依据
- 出现需要你拍板的归属冲突
- 出现目录级重构风险
- 出现跨项目合并风险

除此之外，默认持续向下推进。

## 二、单轮固定回路

每一轮都只跑下面 6 步：

1. 选 1 个节点簇
2. 只读这个节点簇的最小相关文件
3. 判断这一轮只做哪一类治理动作
4. 完成修改
5. 做最小验证
6. 记录结果并自动进入下一簇

## 三、节点簇选择优先级

默认按下面顺序自动选下一簇：

1. 现行主入口仍是旧文件名的簇
2. 仍由历史导航页承担现行入口的簇
3. `entity_ref` 仍挂在合并组、未拆到具体节点的簇
4. 存在明显重复页/同义缺口页的簇
5. 已治理簇中的版本替代与失效清理

这意味着：

- 先修入口
- 再拆壳
- 再细分挂点
- 最后做去重和持续优化

## 四、每轮只允许做的一类动作

单轮只做以下 4 类之一：

- 修正主入口
- 拆历史导航壳
- 重挂原子页
- 补最小节点说明页

禁止一轮内混做太多层级，避免 token 失控。

## 五、自动续跑规则

满足以下条件时，默认直接进入下一轮：

- 当前节点簇已完成最小治理
- 当前簇已存在明确现行入口
- 当前簇没有新增冲突
- 当前轮已完成记录

不需要再等待“是否继续”的确认。

## 六、阶段完成判定

一个项目域在满足以下条件后，从“施工期”转入“巡检优化期”：

1. 主节点页或主说明页已齐
2. 主要历史导航页已降级
3. 大部分原子已挂到具体节点
4. 旧文件名不再充当主入口
5. 版本替代规则已可执行

## 七、巡检优化回路

项目域进入巡检优化期后，默认按下面回路持续维护：

1. 检查新增文件
2. 判断是否替代旧文件
3. 更新受影响节点簇
4. 清理失效关系
5. 记录变更

## 八、对我的默认执行约束

后续我在这个项目里默认遵守：

- 不因无阻塞而停下来等确认
- 不重复做大范围回扫
- 不从头复述已完成历史
- 每轮只汇报新增推进结果
- 每轮结束自动选下一簇继续

## 九、当前状态

从 `2026-08-28` 起，OB 推进默认进入自治循环模式。

### 实际调度补充（2026-08-30）

上述文字是工作规则，本身不能在回复结束后启动执行。2026-08-30已通过应用自动化工具创建当前任务的 heartbeat（ID：`ob`），状态ACTIVE，每4小时接续一次；首次定时执行尚未验证。底层仍分次执行，不是无限运行进程，不需要用户逐次发送“继续”。

每次从状态板唯一下一步续接，轮转四域；局部证据冲突标记待核后继续无阻塞工作。没有新证据或可执行待办时不制造修改、不反复重读。阶段验收后转增量维护。实际运行受调度环境、访问权限及可用额度约束；记录这些限制，不把已配置当成已运行。
