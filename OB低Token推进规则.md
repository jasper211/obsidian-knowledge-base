---
type: methodology
project: OB知识库
status: 生效
as_of: 2026-08-27
owner: Codex
---

# OB低Token推进规则

这份规则用于约束后续 `OB` 知识库治理过程中的读取范围、更新粒度与输出节奏，目标是在不牺牲判断质量的前提下，减少重复读取与无效展开。

## 一、工作原则

- 先判断是否需要新增内容，不默认扩写
- 优先复用现有节点页、说明页、导航页，不重复造页
- 优先修正挂点、命名、关系，再补正文
- 只读取当前变更面，不反复全库扫描
- 只在关系错误、归属错误、命名错误时做治理，不为“更好看”反复改写

## 二、默认推进单位

后续默认以“一个节点簇”为最小治理单位，而不是整域重扫。

一个节点簇通常只包含：

- 1个现行节点页或节点说明页
- 1到3个历史导航页
- 5到20个直接相关原子页

## 三、读取顺序

每次只按下面顺序读取：

1. 当前主节点页
2. 与该节点直接相连的历史导航页
3. `entity_ref` 指向该节点的原子页
4. 明确提到旧文件名或旧挂点的残留页

禁止默认动作：

- 不默认 `rg` 全项目所有相关词
- 不默认重读已经确认稳定的节点页
- 不默认回看整域所有导航页

## 四、更新触发规则

只有满足以下情况之一才更新页面：

- 源文件发生替代、作废、并版
- 当前页挂错节点
- 当前页仍指向旧文件名
- 当前页属于重复壳页
- 当前页内容与现行节点定义冲突

如果只是“表达还能更顺”，默认不改。

## 五、替代与作废处理

当源文件 `B` 替代旧文件 `A` 时，按以下顺序处理：

1. 先更新现行节点页
2. 再更新直接挂在该节点下的状态页/说明页
3. 再检查历史导航页是否仍保留旧入口说明
4. 如果 `A` 已失效且无保留价值，则从关联关系中摘除
5. 如果 `A` 仍有历史解释价值，则降为“历史吸收”或“历史导航”

## 六、输出节奏

每轮只做一类事：

- 修正主入口
- 拆旧导航壳
- 重挂原子页
- 补最小节点说明页

避免一轮里同时跨多个域、多个项目、多个层级混做。

## 七、后续协作约束

后续默认采用以下低 token 协作方式：

- 不重复大段复盘已完成内容
- 不重复解释同一条治理逻辑
- 只汇报“这轮新改了什么、现在能用到哪里”
- 需要复盘时，优先基于已有阶段总结，不从头重讲
- 默认配合 `[[OB自治循环机制]]` 连续推进，不因无阻塞而停下

## 八、当前适用判断

截至 `2026-08-27`，OB 已经可以用于：

- 以节点页为入口给 agent 提供局部业务上下文
- 支持流程分析时沿“节点定义/节点说明 -> 状态页 -> 历史导航页”检索
- 支持对已治理簇做增量更新

但还不适合直接视为“全公司统一稳定知识底座”，因为仍有部分项目和部分旧导航壳未完全拆平。
