# OB知识库_vault · 规则路由

> 本文件是vault根目录的规则手册，会被Claude Code（含Obsidian内的Claudian
> 插件）自动加载。内容刻意保持轻量，只做"路由"——具体某个区域该怎么操作，
> 去对应子目录自己的规则文件找，不在这里堆砌全部细节。

## vault结构与归属边界（铁律，不能越界）

vault顶层4个目录，各自归属不同Agent，**互不越界写入**：

| 目录 | 归属 | 说明 |
|---|---|---|
| `EA流程架构项目/` | OBagent | 从EA项目白名单文档提炼的项目知识原子，**方法论转正Agent/Claudian不要在这里创建或修改文件** |
| `Jasper AI协同经验引擎/` | OBagent | 从"Jasper AI协同经验引擎"代码仓库白名单文档提炼的Agent协同经验知识，**方法论转正Agent/Claudian不要在这里创建或修改文件** |
| `方法论知识库/` | 方法论转正Agent | 对外采集的行业知识（Karpathy LLM-Wiki模式），**这是方法论转正Agent/Claudian唯一应该写入的区域** |
| `MOC/` | 共用 | 导航枢纽，两边都可能需要新增聚合页，改动前先看已有内容避免冲突 |

完整的归属依据、schema兼容契约、双方融合机制，见代码仓库
`05_Agent库/草稿/双Agent协作契约_OB与方法论转正Agent.md`（唯一权威版本，
本文件不重复）。

## 在方法论知识库/行业学习/下工作时

**必须先读`方法论知识库/行业学习/CLAUDE.md`**，里面定义了完整的Karpathy
三层结构（raw只读原始资料/wiki知识网络/规则手册本身）和三项核心操作
（摄入Ingest/查询Query/维护Lint）的具体规则——概念创建阈值、跨文献合并
准则、跟L1-L16方法论笔记交叉比对的要求等，都在那份文件里，不在这里重复。

处理一篇新的行业资料时，默认按该文件定义的Ingest流程走，不需要用户每次
在提示词里重新说明规则。

## 其他区域

`EA流程架构项目/`、`Jasper AI协同经验引擎/`目前只由OBagent的批量提炼
脚本写入，不需要Claude Code/Claudian在对话中主动创建或修改这两个目录下
的文件。如果用户明确要求查阅这两个目录下的内容（检索、回答问题），可以
读，但不要写。
