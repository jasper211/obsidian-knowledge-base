---
type: methodology
project: OB知识库
status: 生效
as_of: 2026-08-28
owner: Codex
---

# OB低Token周监控

这份文档用于判断：`OB` 知识库治理在连续一周后，是否真的显著节省了 token，而不是只是在主观上感觉“更快了”。

## 一、判断原则

不看单次感觉，只看同口径连续记录。

一周后，只要下面 4 项里有 3 项明显改善，就算低 token 策略有效：

- 单轮读取文件数下降
- 单轮修改文件数更集中
- 单轮搜索次数下降
- 单轮总结长度下降，但结果质量不下降

## 二、每轮必须记录的 6 个数

每次推进只记录这 6 个数字：

1. 本轮处理的节点簇数量
2. 本轮读取的文件数
3. 本轮搜索命令数
4. 本轮修改的文件数
5. 本轮是否跨域
6. 本轮是否出现重复回读

## 三、最核心的 3 个周指标

一周后重点只看这 3 个指标：

### 1. 平均读取文件数

目标：

- 从“整簇广扫”下降到“单簇定点读取”
- 理想值：单轮 `6-15` 个文件

如果经常超过 `20`，说明还是在大范围回扫。

### 2. 平均搜索次数

目标：

- 单轮 `1-4` 次检索内完成定位

如果经常超过 `6`，说明节点入口还不稳定，或者我没有遵守低 token 规则。

### 3. 重复回读率

定义：

- 同一页面在 3 轮内被重复完整读取 2 次以上，记为重复回读

目标：

- 一周后重复回读率低于 `20%`

如果还高，说明我们缺“阶段摘要页”或“节点簇索引页”。

## 四、有效节省的判定阈值

相对前一阶段，如果达到下面任意 2 条，就可以判断“确实省了很多 token”：

- 平均读取文件数下降 `30%+`
- 平均搜索次数下降 `30%+`
- 重复回读率下降 `50%+`
- 单轮总结字数下降 `30%+`

## 五、质量守门

不能只看 token 下降，还要同时满足：

- 没有把挂错节点的页留在原处
- 没有把旧文件名继续当主入口
- 没有因为少读而误判归属

也就是说：

- `省 token` 不能以 `误治理` 为代价

## 六、最简单的执行方式

以后每轮结束，只补一行记录，不写长复盘。

记录格式：

`日期 | 节点簇数 | 读取文件数 | 搜索次数 | 修改文件数 | 是否跨域 | 是否重复回读 | 备注`

示例：

`2026-08-28 | 1 | 12 | 3 | 7 | 否 | 否 | HRD-02挂点拆分`

## 七、周复盘方法

每周只做一次简复盘：

1. 统计 7 天记录
2. 算平均读取文件数
3. 算平均搜索次数
4. 算重复回读率
5. 看是否达到阈值

只回答 3 个问题：

- 是不是真的更省了
- 节省主要来自哪里
- 哪一类页面还在浪费 token

## 八、当前执行约束

从 `2026-08-28` 开始，后续 OB 推进默认按这份周监控口径执行。
