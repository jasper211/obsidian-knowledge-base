---
type: 项目笔记
source: 08_任务与跟进/AI上下文
synced: 2026-06-15
tags: [项目]
---

# 任务 1-4：4 表迁移任务取消说明 —— Mark 端 Claude 指南

> 日期: 2026-05-22
> 来源: Carrie（数据底座负责人）
> 目标执行人: Mark / Mark 端 Claude
> 原优先级: P0-P1
> **状态: 已取消 ❌**

---

## 一、背景

原 Week 4 任务包中，任务 1-4 要求将旧版数据库中的 4 张表迁移至 T1 活跃版数据库：

| 原任务 | 表名 | 原目的 |
|---|---|---|
| 任务 1 | `dim_job_family` | 岗位族基础维度 |
| 任务 2 | `fact_activity` | 事实表 |
| 任务 3 | `bridge_l4_job` + `bridge_l3_vs` | 桥接表 |
| 任务 4 | 7 张应用表全迁 | 解锁岗位报告 v2.0 |

---

## 二、取消原因

Carrie 核实后发现以下事实：

### 2.1 两个 SQLite 数据库均已废弃

| 数据库 | 状态 | 说明 |
|---|---|---|
| `数据库_知识库_旧版.db` | ❌ 已废弃 | v4 项目库遗留归档，不再使用 |
| `数据库_知识库_T1活跃版.db` | ❌ 已废弃 | v5 本地知识库副本，不再使用 |

### 2.2 实际生产数据已迁移至 PostgreSQL

- **生产数据库**: `43.98.163.46:5432/mga_platform`
- **岗位信息表**: `process_analytics.DIM_ORG`
- **数据来源**: M3 组织重组后的 7 族 JD 文件（`seed_dim_org.sql`）
- **导入方式**: `import_dim_org.py` 定期导入

### 2.3 旧版表已被新版结构取代

| 旧版表（SQLite） | 新版表（PostgreSQL） | 说明 |
|---|---|---|
| `dim_job_family` | `DIM_ORG` | 岗位族信息，DIM_ORG 数据更完整（7 族全） |
| `fact_activity` | 需确认 | 可能已由其他事实表替代 |
| `bridge_l4_job` | 需确认 | 可能已由 L4 映射脚本替代 |
| `bridge_l3_vs` | `dim_l3.vs_code` | T1 中已存在 |

### 2.4 岗位报告已作废

任务 4 的原目的是"解锁岗位报告 v2.0"，但领导已确认岗位报告不再使用，因此任务 4 的前提已不成立。

---

## 三、Mark 端需同步确认

请 Mark 端 Claude 协助 Mark 确认以下事项：

### 3.1 确认 Mark 本地是否有同样的废弃数据库

检查 Mark 的项目包中是否存在以下文件：

```
<Mark 项目目录>/02_过程成果-工作产出/数据库/数据库_知识库_旧版.db
<Mark 项目目录>/02_过程成果-工作产出/数据库/数据库_知识库_T1活跃版.db
<Mark 项目目录>/02_过程成果-工作产出/数据库/数据库_知识库_T1活跃版_backup_20260509.db
```

**若存在**: 无需做任何迁移操作，保持现状即可。

### 3.2 确认 Mark 端是否有基于旧版数据库的脚本或配置

检查以下路径是否有引用旧版/T1 数据库的脚本：

```bash
# 搜索引用旧版数据库的脚本
grep -r "数据库_知识库_旧版" <Mark 项目目录>/06_Scripts库/ 2>/dev/null

# 搜索引用 T1 数据库的脚本
grep -r "数据库_知识库_T1活跃版" <Mark 项目目录>/06_Scripts库/ 2>/dev/null
```

**若有引用**: 评估是否需更新为指向 PostgreSQL 生产库，或标注为废弃。

### 3.3 确认 Carrie 任务总览更新

Mark 确认后，Carrie 将：
- [ ] 在原任务总览文件标注任务 1-4 取消
- [ ] 在 `00/变更记录/` 追加变更说明
- [ ] 释放任务 1-4 原定工时（~8-13 小时）至其他任务

---

## 四、后续建议

### 4.1 废弃数据库文件处理（可选）

| 文件 | 建议操作 |
|---|---|
| `数据库_知识库_旧版.db` | 保留在 `_归档/` 目录，无需处理 |
| `数据库_知识库_T1活跃版.db` | 可重命名为 `..._已废弃.db` 或保留现状 |
| `数据库_知识库_T1活跃版_backup_20260509.db` | 保留作为历史快照 |

### 4.2 数据底座治理建议

由于旧版 SQLite 数据库已废弃，建议：
1. 以 **PostgreSQL `43.98.163.46`** 作为唯一生产数据源
2. 本地 SQLite 文件仅保留历史归档用途
3. 新增脚本统一连接 PostgreSQL，不再引用本地 `.db` 文件

---

## 五、确认回复

Mark 确认后请回复 Carrie：
- [ ] Mark 端是否存在同样的废弃数据库（是 / 否 / 不确定）
- [ ] Mark 端是否有引用旧版数据库的脚本（有 / 无 / 不确定）
- [ ] 是否同意任务 1-4 正式取消

---

*本指南由 Carrie 于 2026-05-22 创建，供 Mark 端 Claude 协助确认。*
