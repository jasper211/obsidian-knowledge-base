---
type: dashboard
tags: [MOC, 仪表盘]
updated: 2026-07-18
---

# 📊 项目全景仪表盘

> 以下内容由 Dataview 自动聚合，实时更新
> （2026-07-18更新：查询目标从已删除的"项目-流程架构"镜像文件夹
> 改为"EA流程架构项目"知识原子，统计维度换成entity_type/authority_layer/
> confidence这套本体schema迁移新加的字段）

## 📁 按entity_type统计（是否挂上EA自己的DIM_PROCESS/DIM_VS/DIM_ORG/DIM_KPI编码）

```dataview
TABLE length(rows) as 原子数
FROM "EA流程架构项目"
GROUP BY entity_type
SORT 原子数 DESC
```

## 🏛️ 按authority_layer统计（来源权威级别：01原始/02草稿/03已锁定/08任务跟进）

```dataview
TABLE length(rows) as 原子数
FROM "EA流程架构项目"
GROUP BY authority_layer
SORT 原子数 DESC
```

## 🎯 按concept_type统计（规则/决策/定义/经验教训/背景说明）

```dataview
TABLE length(rows) as 原子数
FROM "EA流程架构项目"
GROUP BY concept_type
SORT 原子数 DESC
```

## ⭐ 高置信度原子（confidence非UNSTATED，继承自Mark源文档标注）

```dataview
TABLE source, confidence, decision_status
FROM "EA流程架构项目"
WHERE confidence != "UNSTATED"
SORT confidence DESC
```

## ⚠️ 待复核的孤儿原子

```dataview
LIST
FROM "EA流程架构项目"
WHERE contains(file.content, "待复核")
LIMIT 30
```

## 🔗 知识枢纽概览（483个，详细浏览见[[知识枢纽索引]]）

```dataview
TABLE length(rows) as 枢纽数
FROM "EA流程架构项目"
WHERE contains(file.content, "## 关联原子")
GROUP BY entity_type
SORT 枢纽数 DESC
```

## ✅ 生效原子视图（默认过滤已废止，2026-07-21新增status字段后可用）

> status字段是2026-07-21新加的，只有本次251个结构孤立候选审查后标记的36条
> "已废止"原子有值，其余原子status字段暂缺——Dataview里缺失字段按"不等于
> 已废止"处理，所以下面这个查询目前实际上约等于"全部原子"，等后续治理评分
> （阶段C）陆续给存量原子补上status后，这个视图才会真正起到过滤作用。

```dataview
TABLE authority_layer, confidence
FROM "EA流程架构项目"
WHERE status != "已废止" AND !contains(file.content, "## 关联原子")
LIMIT 50
```

## 🗑️ 已废止原子（标记不删除，保留可追溯）

```dataview
TABLE source, retired_note, superseded_by
FROM "EA流程架构项目"
WHERE status = "已废止"
```

## ⚔️ 待裁定：entity_ref同组矛盾（2026-07-21新增，158个原子/51组）

> entity_ref同编码下多条原子互相矛盾，需要人工判断哪个说法对/如何合并。
> 只标记了组内原子数<=5的"干净小组"；组内原子数>5的组（entity_ref被当成
> 话题标签误用，如"财务流程与凭证"192个原子挤在一个entity_ref下）暂未标记，
> 需要先把这类entity_ref拆分成真正的正式编码颗粒度，再重新跑矛盾检测。

```dataview
TABLE conflict_group, source
FROM "EA流程架构项目"
WHERE status = "待裁定"
SORT conflict_group ASC
```

---
🏠 [[概念笔记层级MOC]] · [[跨项目关联MOC]] · [[知识枢纽索引]]
