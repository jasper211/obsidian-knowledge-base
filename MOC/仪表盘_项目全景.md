---
type: dashboard
tags: [MOC, 仪表盘]
---

# 📊 项目全景仪表盘

> 以下内容由 Dataview 自动聚合，实时更新

## 📁 按层级统计

```dataview
TABLE length(rows) as 文件数
FROM "项目-流程架构"
FLATTEN layer_tag as 标签
GROUP BY 标签
SORT 文件数 DESC
```

## 📎 按附件类型统计

```dataview
TABLE length(rows) as 文件数
FROM "项目-流程架构/附件索引卡"
FLATTEN format as 格式
GROUP BY 格式
SORT 文件数 DESC
```

## 🔥 最近更新的过程产出

```dataview
TABLE layer, subdir, tags
FROM "项目-流程架构"
WHERE layer_tag = "过程"
SORT file.mtime DESC
LIMIT 20
```

## ✅ 已完成的任务

```dataview
LIST
FROM "项目-流程架构"
WHERE contains(file.content, "✅")
LIMIT 30
```

---
🏠 [[流程架构项目MOC]]
