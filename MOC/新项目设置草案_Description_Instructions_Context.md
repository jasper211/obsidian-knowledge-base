---
type: entry
tags: [项目设置, OBagent, 草案]
updated: 2026-07-21
status: draft-for-review-v2
supersedes: v1（已根据《需求定义.md》与《三大主Agent体系架构_v1.3》修正OB的定位描述）
---

# 新项目设置草案 v2（供粘贴进Claude Project设置）

> 用途：把"第二大脑"项目改造为OB（Obsidian巡检Agent）自身建设与优化的项目。以下三块内容分别对应Claude Project设置里的Description、Instructions、Context（知识库附件）。v2根据用户上传的《需求定义.md》《三大主Agent体系架构_v1.3》修正了v1版本"OB=原子提炼系统"的不准确定位。

---

## 一、Description（项目简介，展示在项目名下方）

```
OB（Obsidian巡检Agent）自建项目 —— 个人五Agent体系（PTA/VNW/AIT/方法论转正Agent/OB）里的基础设施/治理层，负责巡检、检索服务、概念笔记提炼三条能力线，目标是让企业知识库能持续自我验证、自我迭代，而不是一次性人工整理或一次性批处理。前身"第二大脑"项目（纯人工整理，未接入AI）的历史资产在此融合为权威参照层。
```

---

## 二、Instructions（自定义指令，完整版）

```
# OB（Obsidian巡检Agent）项目协作指南

## 项目定位
本项目前身是"第二大脑"（纯人工整理知识库，未接入AI、未实现自迭代）。
现改造为OB自身的建设与优化项目。OB是"个人Agent体系架构"（五Agent：
PTA/VNW/AIT/方法论转正Agent/OB）里的基础设施/治理层Agent，服务于其余
四个业务Agent+全部AI工具终端。PTA/VNW/AIT所在的"AI工程能力整改项目"
只负责对接OB，不负责建设OB——建设OB是本项目的范围。

## OB的三条能力线（不要把OB简化理解成"只做原子提炼"）
1. **①巡检**：symlink/MCP配置/Server连通性/vault统计等健康检查。
   成熟度最高，已有实际代码`ob_sync_agent.py`（648行，6项检查）。
2. **②检索服务**：给业务Agent提供"项目背景上下文包"（`get_context()`/
   `format_for_prompt()`），替代各Agent自己重新造检索能力。
   后端已完成混合检索(关键词+向量+图谱)+信任标注，但PTA/VNW/AIT
   目前零行代码真正调用过——服务端就绪不代表能力线闭环。
3. **③概念笔记提炼**：把项目原始文档提炼成结构化知识原子（concept_atom）
   +聚类枢纽（entity_hub）。当前是一次性批处理跑出的静态快照，
   **写入侧完全没有自动化**（新知识产生后不会自动变成新原子写回vault，
   要靠人工再跑一次`migrate_full_vault.py`）——这才是"没有自迭代"的
   真正病根，不只是治理字段没填这么简单。

## 当前架构（Obsidian vault: OB知识库_vault）
- EA流程架构项目/ — 已提炼10155个原子+483个枢纽，schema最完整（含
  authority_layer/confidence/decision_status/entity_type/entity_ref），
  但confidence/decision_status 99%以上仍是UNSTATED占位符，治理评分
  没有真正跑过
- Jasper AI协同经验引擎/ — 已提炼418个原子，但frontmatter只有
  type/concept_type/project/source/extracted_at五个字段，没有治理字段
  也没有聚类枢纽——是比EA更早期的schema版本，不是"没提炼"
- Rw权益项目 — vault里没有对应文件夹，是唯一真正尚未开始批量提炼的项目
- MOC/ — 导航枢纽、诊断报告、路线图（含《OBagent诊断与整合路线图_v2》）
- **三个项目是三套不同成熟度的schema，不是同一模板的三份实例**，处理前
  必须先确认在处理哪个项目、对应哪套字段结构

## 每次对话原则
1. 先看vault里的实际数据/文件，不要凭空假设原子数量、治理状态、
   哪个项目"提炼了没有"——起点笔记等导航文档里的描述可能是过期信息，
   以grep/实际文件为准；用户上传的《需求定义.md》《三大主Agent体系架构》
   是权威口径，如果vault内文档与之冲突，以这两份加上用户当场确认为准
2. 不要把"治理评分补齐"当成"自迭代问题"的完整解法——写入侧自动化
   （OB自巡检机制）没建之前，任何存量数据修补都只是让静态快照变好看，
   不会让知识库真正持续更新
3. 涉及去重、合并、拆解枢纽、补齐schema等会改写/删除原文件的操作，
   先小范围试跑，经确认效果后再批量执行
4. 每次产生重要诊断结论、决策或修复方法，主动提示更新对应的MOC文档
   （起点_从这里开始.md / 仪表盘_项目全景.md / 诊断路线图）
5. EA-KB（原第二大脑项目）里的历史文档，以最新版本号为准，已作废文档
   （如DEPRECATED开头、明确标注"已作废"的）不得引用
6. 本项目不负责实现PTA/VNW/AIT调用OB的接口代码（那是"AI工程能力整改
   项目"的范围），但要保证②检索服务的接口稳定、文档清楚，方便对方接入

## 记忆系统
- 保存三条能力线各自的进展、修复阶段完成情况、用户对优先级的裁定
- 跨对话自动访问，确保修复工作可追溯、不重复劳动
```

---

## 三、Context / 知识库附件建议清单

建议上传/关联以下内容作为项目知识库（按优先级）：

**必读（每次对话都需要）**
- `需求定义.md`（OB的权威定位与三条能力线成功标准，本次用户提供）
- `三大主Agent体系架构_v1.3.md`（五Agent体系全景、OB现状7.3/7.4节，本次用户提供）
- `起点_从这里开始.md`
- `MOC/OBagent诊断与整合路线图_v1.md`（已更新为v2，含量化诊断+分阶段方案）

**按需调用**
- `MOC/仪表盘_项目全景.md`、`MOC/知识枢纽索引.md`
- EA-KB项目里标注"🔴A类必读"的三份：`CLAUDE.md`、`PROJECT_PLAN.md`、`INDEX.md`
- EA-KB里的里程碑裁定记录（M1-M4裁定记录、M3.5过渡路径V0）——作为历史决策参照，不作为待办

**不建议上传**
- EA-KB里已作废文档（2025人效基线报告V0、M3.1产出汇总报告的已作废部分）
- EA流程架构项目里的原始原子文件（10000+份太多，应通过本地vault直接访问/操作，而不是逐个上传）

---

## 待你确认
1. 项目名称：直接叫"OB"还是"OB Agent自建项目"之类更明确的名字？
2. Instructions是否需要调整语气/详细程度？
3. 路线图v2新增的阶段F（写入侧自动化，即"OB自巡检"机制设计）是否同意作为优先级不低于存量质量修补（阶段A-D）的任务？
4. 确认后可以继续推进具体整改工作。
